class Solution {
    public int minOperations(String s) {
        int n = s.length();
        int c = 0;
        int x = 0;
        char ch[] = s.toCharArray();

        // for(int i = 0; i < n - 1; i++){
        //     if(ch[i] == '0' && ch[i+1] != '1'){
        //         ch[i + 1] = '1';
        //         c++;
        //     }
        //     else if(ch[i] == '1' && ch[i+1] != '0'){
        //         ch[i + 1] = '0';
        //         c++;
        //     }
        //     else{
        //         x++;
        //     }
        // }
        // return c;
        for(int i = 0; i < n; i++){
            c += (ch[i] ^ i ) & 1;
        }
        int min = Math.min(c , n-c);
        return min;
    }
}
