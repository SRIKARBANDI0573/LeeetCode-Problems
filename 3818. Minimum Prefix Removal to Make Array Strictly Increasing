class Solution {
    public int minimumPrefixLength(int[] nums) {
        int n = nums.length - 1;
        int c = 0;
        while(n > 0 && nums[n - 1] < nums[n] ){
            n--;
        }
        return n;
    }
}
