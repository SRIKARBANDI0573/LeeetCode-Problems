int addDigits(int num) {
    while(num>=10){
        int sum=0;
        int r;
        while(num>0){
            r=num%10;
            sum=sum+r;
            num=num/10;
        }
        num=sum;
    }
    return num;
}
