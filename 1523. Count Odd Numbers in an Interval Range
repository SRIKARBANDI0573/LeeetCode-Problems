class Solution {
    public int countOdds(int low, int high) {
        // long x[] = new long[high-low+1];
        // for(int i = low; i <= high; i++){
        //     x[i - low] = i;
        // }
        // int c= 0;
        // for(int j = 0; j < x.length; j++){
        //     if(x[j] % 2 != 0){
        //         c++;
        //     }
        // }
        // return c;

        int x = (high + 1) / 2 - low / 2;
        return x; 
    }
}
